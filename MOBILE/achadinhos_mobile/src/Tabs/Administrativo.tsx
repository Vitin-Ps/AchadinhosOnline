import { Text, VStack } from "native-base";

export default function Administrativo({ navigation }) {
    return (
        <VStack>
            <Text>Page_Administrativo</Text>
        </VStack>
    )
}
